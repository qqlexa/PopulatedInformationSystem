spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/information_system
    username: nastya
    password: mypassword
    driver-class-name: org.postgresql.Driver